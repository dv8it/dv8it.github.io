import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#051e3e',
    },
    secondary: {
      main: '#851e3e',
    },
    text: {
      primary: '#000',
      secondary: '#fff',
    },
  },
});

export default theme;
